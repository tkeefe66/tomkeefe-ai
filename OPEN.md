# OPEN — clauses omitted for missing values (rewrite brief, 2026-08)

Per the brief's rule 1: unfilled values are never estimated or padded — the
clause is deleted and logged here. Restore each by editing the content module
listed.

Resolved 2026-08-05: B4 ("2 leagues in beta"), B5 ("over 1,000 items
catalogued"), and B7's scale + what-broke fragments (derived from repo
history) are now live in `content/projects.ts`. The footer changelog
("Last shipped: [month], [what]") was dropped by decision — the footer stays
"© 2026 Tom Keefe" alone, permanently.

Still open:

- **B7 cost fragment** (`content/projects.ts`, tomkeefe.ai meta): API spend /
  running cost unfilled — meta row ships with 2 of 3 fragments.
- **B1/B2 — Life Tracker** (deferred by decision, not blocked): card ships
  body-only with no metadata row; /projects/life-tracker is an intentional
  stub. Restore target: before the Dynasty launch, September 2026.
- **MarTech detail page — "WHAT CHANGED" section** (`content/projects.ts`,
  removed in Phase 2): its body was a literal bracket placeholder awaiting a
  real outcome metric. Reinstate when the number exists.
- **Stub routes** (`/projects/life-tracker`, `/projects/dynasty-analyzer`,
  `/projects/tomkeefe-ai`): intentionally short — card copy + short intro
  only, no invented narratives. Full writeups pending source material.
- **Inventory FIG. 01 caption** still reads "491 RECORDS" — accurate to the
  screenshot it describes, but the capture predates the 1,000-item ledger.
  Refresh the screenshot (and caption) when convenient.
